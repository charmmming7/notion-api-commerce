export type TSlug = {
  _type: string;
  current: string;
};

export type IProduct = {
  // image: any;
  name: string;
  slug?: TSlug;
  // price: number;
  // discount?: number;
  // details?: IProductDetails[];
  // brand: string;
  // category: string[];
  // isOffer?: boolean;
  // registerDate?: string;
  // timeStamp?: number;
  // starRating: number;
};
